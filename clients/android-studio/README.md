# Mini-O Android Studio client

This directory is an Android Studio IntelliJ Platform plugin project. Its first
working action sends selected editor text through MCP. Project sync, build,
and Logcat context still require explicit redaction and approval work before
they are enabled.
