# Mini-O JetBrains client

This directory is now an IntelliJ Platform plugin project. Run `./gradlew
buildPlugin` after generating or providing the Gradle wrapper, then install the
ZIP from the IDE Plugins settings. The editor action sends selected text only;
the endpoint is intentionally shared with Mini-O rather than JetBrains-specific.
